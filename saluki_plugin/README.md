Saluki plugin — small package used for integration testing and examples.

This tiny README exists so editable installs of this package via `pip -e` can
find a README artifact and avoid warnings during packaging.
